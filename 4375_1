#입력
while True:
  try:
    n = int(input())
    s = 1 #1로만 이루어진 수
    cnt = 1 #자릿수
    while True:
      if s%n == 0: #s가 n의 배수인가?
        print(cnt)
        break
      else:
        s += 10**cnt
        cnt += 1
  except:
    break
  
